#! /usr/bin/env python
import os
import sys

import baca

if __name__ == "__main__":
    directory = os.getcwd()
    undo = "--undo" in sys.argv[1:]
    baca.build.color_persistent_indicators(directory, undo=undo)
