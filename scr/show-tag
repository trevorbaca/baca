#! /usr/bin/env python
import os
import sys

import baca

if __name__ == "__main__":
    directory = os.getcwd()
#    arguments = baca.build.arguments("--tag", "--undo")
#    if not arguments.tag:
#        print("Missing mandatory tag parameter ...")
#        sys.exit(-1)
#    messages =baca.build.show_tag_function(directory, arguments.tag, undo=arguments.undo)
    # messages = baca.build.show_tag_function(directory, "MEASURE_NUMBER")
    messages = baca.build.show_tag_function(directory, "LOCAL_MEASURE_NUMBER", undo=True)
    print(messages)
